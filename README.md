# Project Observatory

Cross-project, read-only observatory for reconstructing canonical state, dependencies, uncertainty, and external decision triggers across selected projects.

> **Bootstrap provenance note:** This repository was created empty. This README exists solely to establish the initial Git ref required by the connected GitHub interface. No observed-project repository was inspected and no scientific or product conclusion was drawn before the separately committed Observatory method freeze.

The governing method, schemas, and registers are frozen separately before canonical reconstruction begins.
