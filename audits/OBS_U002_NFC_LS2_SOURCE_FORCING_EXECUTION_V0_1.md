# OBS-U002 — NFC LS-2 Source-Forcing Audit Execution V0.1

Status: `EXECUTED__OUTCOME_C`

Operation: `OBS_U002_NFC_LS2_SOURCE_FORCING_AUDIT_EXECUTION_V0_1`

Preregistration parent: `ee0623aca69b5ea133ec6060f979fa2a146f5b17`

Selected uncertainty: `OBS-U002`

Controlling outcome:

```text
C__SOURCE_NONFORCING_ESTABLISHED_BY_COUNTERMODEL_OR_INDEPENDENCE_ARGUMENT
```

## 0. Boundary and integrity

Execution was performed read-only against the frozen NFC scientific canon:

```text
REPOSITORY = etblink/Nested-Fibrational-Cosmology
CANONICAL_REF = archive/nfc-canonical-ed3047c2
CANONICAL_COMMIT = ed3047c2cbc0abc34d2549dd27754e4d3d05af78
CANONICAL_ROOT_TREE = 00ef55ff36d5e9663ca1ef2c9566e2bc1396f973
```

No NFC file, branch, theorem, status, ledger, or canon object was modified. No external literature was searched. The later noncanonical foundational-reduction continuity record was not used as evidence for the controlling result.

## 1. Source map — controlling findings

The seven-book frozen spine was inspected under the preregistered vocabulary and dependency-neighbor rule. The load-bearing map is:

### Book I — primitive quotient/stabilization only

Book I proves finite observational quotient/refinement/stabilization machinery, but explicitly denies that it proves locality or boundary determinacy. Its own boundary section says observable outcomes in a region are not shown to be controlled by boundary data and that this requires the LS-2 package of Book II. Its holographic/interface sparsity statement explicitly imports LS-2 from Book II.

Disposition: Book I establishes P2/P3 and finite-alphabet ingredients, not P5.

### Book II — sole attempted source of P5

Book II first introduces LS-2 explicitly as a `[C]` hypothesis:

```text
there exists a finite radius rho* such that ... the observable local outcome on U
is determined by the stabilized collar type on partial_{rho*} U.
```

Book II then gives two routes toward LS-2:

1. `DW + TC + LAR -> LS-2` (`thm:ls2-from-conditions`, conditional);
2. `HC2 -> UNR -> LA2 -> LS-2` (`prop:HC2-ls2`, conditional on HC2 certification).

The first route contains a circular attempted universalization. `cor:ls2-universal-K07` is tagged `[U]` but explicitly assumes a `Phase-A` substrate. `Phase-A`, by `def:structural-phases`, is defined to require **LS-2 certification**. The proof then obtains LAR from `DW + Phase-A` and returns to LS-2. Thus the attempted source-forcing chain uses P5-certification inside a premise needed to derive P5.

This is not a valid antecedent derivation under the audit preregistration or Book VII's own no-status-inflation law.

The HC2 route is a genuine sufficient-condition route but is expressly conditional on separately certified `HC2`; the frozen source does not make HC2 a universal antecedent consequence of the Book-I kernel.

Disposition: Book II proves valid conditional P5 consequences and supplies sufficient conditions, but no noncircular universal source forcing from the antecedent kernel.

### Book III

Book III repeatedly imports the Book-II boundary law **under LS-2** and builds interface burden, boundary defect, and the Unified Coercive-Transport machinery on it. It supplies no independent LS-2 discharge.

Disposition: downstream consumer of P5.

### Book IV

Book IV includes the Book-II boundary-law map under LS-2 inside POT fiber/transport structure and source invariants. It supplies no independent LS-2 derivation.

Disposition: downstream consumer of P5.

### Book V

Book V's LS-2-bearing sector/channel results remain explicitly conditional on LS-2 (often together with PASS, Phase-A, and K0=7). No independent finite-interface sufficiency theorem appears.

Disposition: downstream consumer of P5.

### Book VI

Book VI defines flux/continuum-interface structures using the LS-2 boundary law and states later continuum eligibility/refinement results conditional on LS-2. It supplies no independent discharge.

Disposition: downstream consumer of P5.

### Book VII

Book VII contains no independent LS-2 theorem. Instead it controls theorem force: conditional results do not become unconditional by repetition/proximity; scoped results do not self-promote; promotions require separately proved transfer theorems.

Disposition: governance blocks the circular or downstream reuse of LS-2 from being counted as an antecedent proof of LS-2.

## 2. Premise ledger for the strongest positive route

The smallest source-faithful antecedent set capable of making the strongest positive case consists of:

| ID | Source | Status | Role |
|---|---|---|---|
| P-A1A4 | Book I `def:admissible-class` | D | admissible process/test discipline |
| P-Q | Book I observational quotient package | U/D | quotient visibility and no-smuggling |
| P-STAB | Book I `thm:stabilization` | U | finite refinement stabilization |
| P-CG | Book I CG-1..CG-6 | D | finite alphabet/fibers/projection/nesting/valence gates |
| P-K0 | Book I collar alphabet result | U/D | finite canonical collar alphabet; source asserts K0=7 |
| P-DW | Book II `thm:DW-from-A2` | U as stated | deterministic collar-update behavior from structural invariance |
| P-TC | Book II `sr:toggle-complete`, `thm:TC-from-sr` | D/U | complete adjacent collar toggles |
| P-LAR | Book II `def:LAR` / `thm:LAR-from-DW-PhA` | D/C | separation of distinct collar classes |
| P-HC2 | Book II `prop:HC2-ls2` | C | alternate sufficient condition, not universal antecedent |
| P-PHASEA | Book II `def:structural-phases` | D | includes LS-2 certification; therefore target-containing |

The preregistered distinctions remain:

```text
P1 = bounded/local propagation or update dependence
P2 = local detectability by an admitted test family
P3 = finite stabilization of refinement/quotient structure
P4 = finite interface/collar alphabet
P5 = finite-interface sufficiency / bounded-collar factorization
```

No noncircular source theorem establishes `P1 + P2 + P3 + P4 -> P5`.

## 3. Strongest positive derivation attempt

### Route A — DW + TC + LAR

`thm:ls2-from-conditions` is a valid conditional schema if DW, TC, and LAR are independently available with the force its proof needs. However the frozen canonical route used to universalize LAR invokes Phase-A, and Phase-A already requires LS-2 certification. Therefore the purported universal discharge is circular with respect to the target.

In addition, the logical content of LAR is collar-class separability: distinct collar classes can be distinguished by short tests. That alone does not imply that identical collar classes force identical interior local outcomes. The proof's move from deterministic collar updates/separable collar classes to equality of all stabilized interior outcomes requires exactly the missing sufficiency implication.

### Route B — HC2

`HC2 -> UNR -> LA2 -> LS-2` is a separately stated conditional sufficient-condition route. It does not force LS-2 unless HC2 is independently established for the claimed source universe. No such universal source antecedent is supplied by the frozen spine.

### Positive-pass result

A genuine partial positive result survives:

```text
IF a regime independently certifies LS-2-equivalent collar sufficiency
(or an explicitly sufficient condition such as HC2 / a valid independent LAR package),
THEN Book II's boundary determinacy and finite-capacity consequences follow.
```

This is a conditional interface theorem, not source forcing.

## 4. Adversarial nonforcing construction

### 4.1 Model family

For each sufficiently large finite region size `m`, construct a finite relational substrate over a finite graph signature with:

- an undirected adjacency relation `E`;
- a finite seven-symbol collar-color relation, giving exactly seven stabilized collar-symbol classes;
- a finite site set and finite valence;
- a designated region `U_m` with an exterior collar;
- two configurations `omega_m^0` and `omega_m^1` that are **identical on every exterior collar neighborhood of U_m** and differ only by a structural motif strictly inside `U_m`.

Choose the interior difference structurally, not by labels:

- `omega_m^0`: the deep interior contains no 3-cycle at the chosen local motif;
- `omega_m^1`: the corresponding deep interior contains a 3-cycle.

All boundary/collar relational data are identical between the two configurations.

### 4.2 Licensed local question

Include the structurally invariant local test

```text
T_triangle(U) = 1 iff the induced relational graph on U contains a 3-cycle,
                0 otherwise.
```

This test:

- is invariant under graph isomorphism;
- is readable entirely from the base relational signature;
- uses no external naming, primitive coordinate, token identity, or hidden memory;
- has finite output;
- therefore satisfies the Book-I anti-smuggling/admissibility discipline.

The stabilized local outcomes of the two configurations differ because `T_triangle` distinguishes them.

### 4.3 Admissible process class

Take the admissible class generated by:

- identity/typed composition where lawful;
- all adjacent single-site collar-color toggles required by Book-II toggle completeness;
- structurally invariant finite-output tests including `T_triangle`;
- their typed compositions.

Because the universe is finite and every generator is structurally invariant with finite output, A1--A4 are satisfied. Toggle completeness is satisfied by construction.

### 4.4 Quotient/refinement/stabilization

Use a finite test family that contains the collar tests and `T_triangle`. The observational quotient is finite. Once these tests are present, the quotient stabilizes immediately (or after a finite fixed number of stages), satisfying the Book-I finite stabilization theorem and its quotient discipline.

Every equivalence class/fiber is finite; projection terminates; nesting and valence are finite; the stabilized collar alphabet is finite and chosen with seven collar classes. Thus the relevant finite structural gates are preserved rather than evaded.

### 4.5 DW, TC, and LAR can all hold

- **DW:** equal stabilized collar data give the same collar partition update under every collar toggle. The interior triangle distinction does not create a different collar update.
- **TC:** all required adjacent collar toggles exist by construction.
- **LAR:** the seven distinct collar colors/classes are independently distinguishable by short structural collar tests. Thus distinct collar classes are locally addressable.

These properties constrain the **collar dynamics and collar distinguishability**. They do not erase an independently admissible quotient-visible distinction in the deep interior.

### 4.6 Failure of P5

For the two configurations:

```text
c_U(omega_m^0) = c_U(omega_m^1)
```

for the stabilized collar statistic, while

```text
q_U(omega_m^0) != q_U(omega_m^1)
```

because `T_triangle` gives different stabilized local observable outcomes.

Hence no map `Phi_U` can satisfy

```text
q_U = Phi_U o c_U
```

on this model.

The failure is finite-interface sufficiency itself; it is not a failure of finiteness, stabilization, locality of collar updates, local collar addressability, or no-smuggling.

### 4.7 Uniform-radius strengthening

A parameterized version places the structurally distinct motif arbitrarily deep in larger finite regions while keeping the complete exterior collar data fixed. Thus, for every proposed source-uniform bounded collar radius, sufficiently large finite members retain identical bounded collar data with distinct admissible stabilized interior outcomes.

This supplies the preregistered family-style non-implication test in addition to the single-model failure.

## 5. Countermodel admissibility checklist

```text
FINITE_RELATIONAL_SIGNATURE = YES
FINITE_CONFIGURATION_SPACE_PER_MODEL = YES
FINITE_VALENCE = YES
FINITE_FIBERS = YES
TERMINATING_PROJECTION = YES
FINITE_STABILIZED_COLLAR_ALPHABET = YES
K0_7_COMPATIBLE_COLLAR_ALPHABET = YES
A1_TYPED_COMPOSITION = YES
A2_STRUCTURAL_INVARIANCE = YES
A3_BASE_SIGNATURE_ACCOUNTABILITY = YES
A4_FINITE_OUTPUT_CONTROL = YES
NO_LABEL_SMUGGLING = YES
OBSERVATIONAL_QUOTIENT_PRESERVED = YES
FINITE_REFINEMENT_STABILIZATION = YES
DW_COMPATIBLE = YES
TC_COMPATIBLE = YES
LAR_COMPATIBLE = YES
P5_BOUNDED_COLLAR_FACTORIZATION = NO
```

The model therefore satisfies the antecedent structural content relevant to the purported forcing route while violating P5.

## 6. Equivalence and scope audit

The frozen spine does contain genuine interface-related positive consequences, but they fall into one of three classes:

1. conditional on LS-2 itself;
2. conditional on an additional sufficient condition such as HC2;
3. downstream counting/transport consequences once boundary factorization is already available.

None independently forces the preregistered `LS2_EQUIVALENT` predicate from the antecedent frozen source kernel.

The circular `Phase-A -> LAR -> LS-2` universalization is a source-level proof defect relevant to the positive route, but it does **not** invalidate adjudication of the target because the independent countermodel establishes non-implication directly. Therefore outcome E does not control.

## 7. Controlling adjudication

The preregistered Section-7 negative criterion is satisfied by an explicit lawful countermodel and its uniform-radius family strengthening.

Therefore:

```text
CONTROLLING_OUTCOME =
C__SOURCE_NONFORCING_ESTABLISHED_BY_COUNTERMODEL_OR_INDEPENDENCE_ARGUMENT
```

Subsidiary result:

```text
PARTIAL_POSITIVE_RESULT =
VALID_CONDITIONAL_FINITE_INTERFACE_THEOREMS_SURVIVE_WHEN_SUFFICIENCY_IS_INDEPENDENTLY_CERTIFIED
```

This does not alter the controlling C result because the target was universal/source forcing of LS-2-equivalent finite-interface sufficiency.

## 8. Scientific meaning

At the frozen boundary, NFC's quotient, finiteness, stabilization, finite collar alphabet, collar dynamics, and local collar addressability do not by themselves imply that all admissible stabilized interior observable content factors through bounded collar data.

Accordingly, finite-interface sufficiency is not source-forced by the audited antecedent structure. It remains a separately certified regime condition/sufficient-condition result wherever it is lawfully used.

This is an Observatory finding only. It does not rewrite NFC canon and does not authorize an NFC repair.

## 9. Stop state

```text
OBS_U002_AUDIT_EXECUTED = YES
OBS_U002_OUTCOME_ADJUDICATED = YES
CONTROLLING_OUTCOME = C__SOURCE_NONFORCING_ESTABLISHED_BY_COUNTERMODEL_OR_INDEPENDENCE_ARGUMENT
NFC_REPOSITORY_MUTATED = NO
NFC_CANON_MODIFIED = NO
EXTERNAL_LITERATURE_SEARCH = NO
REPAIR_PROPOSED = NO
OBS_U003_STARTED = NO
```
