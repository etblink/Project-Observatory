# Acceptance — NFC NS Internal Control Premise Minimality Gate v0.1

STATUS: ACCEPTED__GATE_CLOSED

PREREGISTRATION_COMMIT: `de1364024b201fd6c3674e4db4dc9048ad7d36a2`
EXECUTION_COMMIT: `83c785085152b7b6445b306668529025f09b0358`
BASE_ACCEPTANCE: `668c8ae664e95cf5bd1c498589ccc3c09d4b695a`

## 1. Acceptance standard

This acceptance asks whether the execution faithfully adjudicated the frozen candidate set against the preregistered question and stop rule. It does not import later Observatory results as controlling evidence, mutate NFC, or promote a research premise into frozen source truth.

Acceptance requires:

1. the inherited source-forcing result is preserved;
2. value control, oscillation-only control, and relative absorption are not conflated;
3. all C1-C6 candidates are evaluated;
4. minimality is judged against the actual inherited application target;
5. no hidden event reconstruction, IDC import, withdrawn generic ceiling, or post-hoc contraction coefficient is used;
6. any selected premise remains prospective new content;
7. contraction/common-state closure is not claimed.

## 2. Acceptance checks

### A1 — Lineage: PASS

The execution is exactly parented to the preregistration lineage and remains bound to base acceptance `668c8ae664e95cf5bd1c498589ccc3c09d4b695a`.

### A2 — Inherited negative result: PASS

The execution correctly preserves the accepted source-forcing result: per-step finiteness does not force one tail-uniform internal event alphabet, multiplicity bound, or resulting uniform value ceiling. Therefore C6 cannot satisfy the gate.

### A3 — Target decomposition: PASS

The execution distinguishes:

```text
T1 = absolute value control
T2 = oscillation-only control
T3 = relative / absorbable control
```

This distinction is necessary. In particular, a direct oscillation bound is invariant under additive shifts and therefore cannot by itself control the absolute level of `E_n`.

### A4 — C1 sufficiency: PASS

With `E_n >= 0`, the prospectively fixed ceiling

```text
0 <= E_n <= M_E < infinity
```

is sufficient for the inherited value-control target and immediately implies finite within-window oscillation. No event-class reconstruction is required.

### A5 — C2 nonminimality: PASS

The finite event envelope supplies more structure than the gate requires. Its relevant consequence here is a derived finite ceiling; therefore C2 is stronger than the direct C1 premise for this target.

### A6 — C3 nonminimality: PASS

`E_n <= C_E B_n` introduces a cross-channel domination relation and only yields an absolute value bound when independent boundary boundedness is also available. This is more structured than C1 and does not automatically imply absorption.

### A7 — C4 overstrength: PASS

Same-state relative domination is aimed at the stronger T3 target. It may be useful for later absorption, but it is not minimal for mere value control and its coefficient may not be chosen post hoc to force contraction.

### A8 — C5 subsidiary minimality: PASS

The execution correctly identifies C5 as strictly weaker than C1 for an oscillation-only target. The additive-shift family proves that C5 does not imply T1. Therefore it cannot control this gate while the inherited application requirement remains value control.

### A9 — Outcome selection: PASS

The controlling outcome is correctly assigned:

```text
A__ABSOLUTE_INTERNAL_CEILING_IS_MINIMAL_FOR_VALUE_CONTROL__NOT_FOR_ABSORPTION
```

with the preserved subsidiary:

```text
IF_TARGET_IS_NARROWED_TO_OSCILLATION_ONLY:
B__DIRECT_OSCILLATION_PREMISE_IS_STRICTLY_WEAKER_AND_MINIMAL
```

### A10 — Governance / stop rule: PASS

The selected ceiling is explicitly a research hypothesis. It is not a frozen NFC theorem, does not discharge same-state absorption, common-state completion, or contraction, and does not mutate NFC/FCP/PGH.

## 3. Accepted result

```text
ACCEPTED_OUTCOME =
A__ABSOLUTE_INTERNAL_CEILING_IS_MINIMAL_FOR_VALUE_CONTROL__NOT_FOR_ABSORPTION
```

The accepted prospective premise is:

```text
P_NS_INTERNAL_VALUE_CEILING:
There exists a prospectively fixed or independently derived finite M_E
such that 0 <= E_n <= M_E on the declared late-tail regime.
```

Its force is limited to finite absolute value control and the resulting finite oscillation bound.

It does not imply relative smallness, absorbability, or eventual contraction.

## 4. Closure state

```text
NFC_NS_INTERNAL_CONTROL_PREMISE_MINIMALITY_GATE = ACCEPTED__CLOSED
C1_SELECTED = YES
C1_IS_NEW_RESEARCH_PREMISE = YES
C1_IS_FROZEN_NFC_THEOREM = NO
OSCILLATION_ONLY_C5_SUBSIDIARY = PRESERVED
RELATIVE_ABSORPTION_DERIVED = NO
CONTRACTION_DERIVED = NO
SOURCE_PROJECT_MUTATED = NO
```

No additional scientific operation is selected by this acceptance.