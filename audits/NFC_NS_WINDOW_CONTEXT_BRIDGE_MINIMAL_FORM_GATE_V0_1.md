# NFC NS Window Context Bridge Minimal Form Gate v0.1

PREREGISTRATION: 1a718220309c2bf5b4a77056eb9a0f4883bab8af
PROVISIONAL_HYPOTHESIS: P_NS-FORCE-PAIR-REP

## Outcome
B__M1_IS_MINIMAL_NEW_DECLARATION__AGGREGATE_CONTEXT_DERIVED

## Finding
The active NS averaging operator is indexed by transport stages j in W_k, with weights inherited from the Book-III transport factors. Book III already defines composable transfer chains as typed context maps

(U_j,Sigma_j) -> (U_{j+1},Sigma_{j+1}).

The weakest additional statement is therefore not a new physical-region assignment. It is the explicit declaration that the active NS contraction profile and window index set are carried by one such declared composable transfer chain.

Define the minimal bridge:

T_NS-WINDOW-CONTEXT-MIN:
For every active transport index j appearing in the NS window family, there exists a declared member (U_j,Sigma_j) of the active composable Book-III transfer chain whose transport factor supplies the corresponding NS contraction/weight datum, and this assignment is presentation invariant and successor compatible.

Then set Ctx(j)=(U_j,Sigma_j).

For a window W_k, its aggregate source/transport context need not be postulated independently. It is the finite indexed family

Ctx(W_k) := { Ctx(j) : j in W_k }

together with the declared transfer maps among those members. Any later continuum-region summary must be earned through Book VI/Phi and need not exist as one primitive union object.

## Candidate adjudication
- M1: ACCEPTED as minimal new declaration.
- M2: unnecessary strengthening; a primitive aggregate context adds structure not required for incidence.
- M3: rejected as premature continuum/spacetime import.
- M4: rejected as response inversion/post-outcome localization.

## Why Outcome A is not warranted
Frozen NS states that the active window profile is determined by source-descended transport factors and that averaging uses j in W_k, but it does not explicitly bind those j values to one named composable Book-III transfer chain with context objects U_j. That binding is semantically natural but not written as a frozen theorem. It must therefore be declared/proved rather than silently inferred.

## Consequence
Once T_NS-WINDOW-CONTEXT-MIN is provisionally supplied, source localization can be formulated without physical-scale/window-index conflation:

F_src -> source component alpha -> incidence with one or more Ctx(j) -> S_F(W_k).

Quantitative source burden and source transport weights remain separate downstream burdens.

## OpenAI control
No smallness, regularity, or outcome-dependent condition is introduced.

No source-project mutation and no forced-regularity claim.